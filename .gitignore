# .NET build output
bin/
obj/
TestResults/
*.binlog

# IDE and per-user state
.vs/
.idea/
*.user
*.suo
launchSettings.json

# Native kernel build output (rebuilt by scripts/build-kernels.ps1)
src/Compositor.Kernels/bin/

# Packaging output
dist/
*.zip
*.exe
!scripts/**/*.exe

# Model downloads and local caches (Remove Background fetches its model at first use)
models/

# Secrets and signing material — never commit
*.pfx
*.p12
.env

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Claude Code settings personal to this machine
.claude/settings.local.json
BUILD_PLAN.md
